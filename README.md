# a_openclaw_config

This repository tracks my OpenClaw workspace/config notes.

## What’s inside

- `USER.md`: user preferences (how to address me, tone, typical requests)
- `IDENTITY.md`: assistant identity stub
- Other workspace docs may be added over time

## Notes

This repo may contain personal workflow preferences. Avoid committing secrets (tokens, private keys, passwords).
